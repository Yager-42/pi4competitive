# earendil_works.pi_agent

Python isomorphic port of upstream `packages/agent` from
[earendil-works/pi](https://github.com/earendil-works/pi) (`main`).

Import: `import earendil_works.pi_agent as pi_agent`

C-tier surface: core agent loop + harness (session JSONL, compaction, skills, tools).

See `docs/plans/P2_packages_agent.md` and `docs/contracts/ARCHITECTURE_CONTRACT.md`.
